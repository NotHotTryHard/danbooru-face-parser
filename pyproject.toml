[project]
name = "danbooru-face-parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "huggingface-hub>=1.12.2",
    "pyyaml>=6.0.3",
]

[project.scripts]
danbooru-face-parser = "processor:main"
